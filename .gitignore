node_modules/
dist/
*.tgz
.env
.env.*

# Local agent/session state; keep out of public repos.
AGENTS.md
memory/
.playwright-mcp/

# Internal development/planning notes; keep out of the public repo.
docs/superpowers/
docs/design.md
docs/research.md
docs/todo.md

# Local bridge runtime state: tokens, HMAC key, sessions, results, artifacts.
# Defense-in-depth even if .bridge/.gitignore is ever deleted; the tracked
# .bridge/.gitignore itself is preserved via the negation.
/.bridge/*
!/.bridge/.gitignore

# Editor / OS junk
.DS_Store
.idea/
.vscode/
*.log

# Demo GIF frame sources (the rendered assets/demo.gif is tracked)
assets/demo/

# Local agent/session config (never publish)
.claude/
